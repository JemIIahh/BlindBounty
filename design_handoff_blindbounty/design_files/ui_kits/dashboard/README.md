# Dashboard UI Kit

Recreation of the `dashboard/` app surface — monochrome Roboto-Mono terminal aesthetic, sharp corners, industrial grid background.

- `primitives.jsx` — `DIcon`, `Sidebar`, `TopBar`, `StatTile`, `TaskRow`, `TaskTable`, `DetailPanel`
- `index.html` — interactive browse → select → inspect flow with 7 mock tasks.
